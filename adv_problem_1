class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        result = []
        count = 0
        for i in range(len(nums)):
            count += nums[i]
            result.append(count)
        return result
